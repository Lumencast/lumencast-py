"""``python -m lumencast`` CLI surface.

Subcommands :

- ``serve-scenario`` — boot the LSDP/1 server with the interop control plane.
- ``conformance`` — drive a remote server through the scenario suite.
- ``validate`` — schema-check an LSML bundle.
- ``build`` — canonicalise + hash a bundle.
"""

from __future__ import annotations
